## Packages
recharts | Data visualization for dashboard stats
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Date formatting and manipulation for attendance/payments
clsx | Utility for constructing className strings conditionally

## Notes
Authentication uses session cookies (credentials: "include")
Role-based access control implemented in frontend layout and routing
